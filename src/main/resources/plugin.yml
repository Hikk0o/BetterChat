name: BetterChat
version: '${project.version}'
main: hikko.betterchat.BetterChat
load: POSTWORLD
api-version: 1.19
prefix: BetterChat
authors: [ Hikko ]
depend:
  - Essentials
  - AuthMe
  - ProtocolLib
  - SuperVanish

commands:
  dmsg:
    description: " "
  allowportal:
    description: " "

permissions:
  betterchat:
    detelemessage:
      default: op
      description: Allow delete messages